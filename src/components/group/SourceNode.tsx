import './source-node.css'

export default defineComponent({
  name: 'SourceNode',
  setup: () => {
    // eslint-disable-next-line react/display-name
    return () => {
      return (

        <div className="ax6-source-node">
          来源source
        </div>

      )
    }
  },
})
